# Deduper CPU dependencies
# Uses CPU-only PyTorch wheels to keep image size small

--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple
torch~=2.11.0
torchvision~=0.26.0

-r requirements-base.txt
